<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Never Lose Another Hot Lead to a Missed Call</title>
  <meta name="description" content="AI instantly texts missed callers, qualifies them, and books callbacks automatically." />

  <style>
    :root{
      --bg:#0b0f19;
      --card:#111827;
      --muted:#9ca3af;
      --text:#f9fafb;
      --accent:#22c55e;
      --accent2:#60a5fa;
      --border:rgba(255,255,255,.08);
      --shadow:0 20px 60px rgba(0,0,0,.35);
      --radius:18px;
      --max:1100px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
      background:
        radial-gradient(1200px 600px at 20% -10%, rgba(34,197,94,.25), transparent 60%),
        radial-gradient(900px 500px at 90% 10%, rgba(96,165,250,.20), transparent 55%),
        var(--bg);
      color:var(--text);
      line-height:1.45;
    }
    a{color:inherit;text-decoration:none}
    .container{max-width:var(--max); margin:0 auto; padding:24px}
    .nav{display:flex; justify-content:space-between; align-items:center}
    .brand{display:flex; gap:10px; align-items:center; font-weight:700}
    .logo{
      width:34px;height:34px;border-radius:10px;
      background:linear-gradient(135deg,#22c55e,#60a5fa);
    }
    .navlinks{display:flex; gap:16px}
    .navlinks a{font-size:14px;color:var(--muted)}
    .navlinks a:hover{color:var(--text)}
    .btn{
      padding:12px 16px;
      border-radius:14px;
      border:1px solid var(--border);
      background:rgba(255,255,255,.04);
      font-weight:600;
    }
    .btn.primary{
      background:linear-gradient(135deg,#22c55e,#16a34a);
      color:#052e16;
      border:none;
    }
    .hero{
      display:grid;
      grid-template-columns:1.15fr .85fr;
      gap:26px;
      padding-top:30px;
    }
    @media(max-width:900px){.hero{grid-template-columns:1fr}}
    h1{font-size:44px;margin:14px 0}
    .sub{font-size:18px;color:#d1d5db}
    .bullets{list-style:none;padding:0;margin:16px 0}
    .bullets li{margin-bottom:10px}
    .card{
      background:rgba(17,24,39,.75);
      border:1px solid var(--border);
      border-radius:var(--radius);
      padding:18px;
    }
    input{
      width:100%;
      padding:12px;
      border-radius:12px;
      border:1px solid var(--border);
      background:rgba(255,255,255,.03);
      color:var(--text);
    }
    .section{padding:40px 0}
    footer{padding:30px 0;color:var(--muted);font-size:13px;border-top:1px solid var(--border)}
  </style>
</head>

<body>

<!-- RENDER CONFIRMATION -->
<div style="padding:18px;background:#fff;color:#000;font-size:18px;">
  ✅ If you can see this, GitHub Pages is rendering correctly.
</div>

<div class="container">

  <div class="nav">
    <div class="brand">
      <div class="logo"></div>
      MissedCallAI
    </div>
    <div class="navlinks">
      <a href="#how">How it works</a>
      <a href="#demo" class="btn">Book demo</a>
    </div>
  </div>

  <div class="hero">
    <div>
      <h1>Never lose another hot lead to a missed call.</h1>
      <p class="sub">
        When you miss a call, your AI assistant texts instantly, qualifies the lead,
        and books a callback before they move on.
      </p>

      <ul class="bullets">
        <li>✓ Instant missed-call text back</li>
        <li>✓ Sales-style qualification</li>
        <li>✓ Booked callbacks, not voicemails</li>
      </ul>

      <a class="btn primary" href="#demo">See the demo</a>
    </div>

    <div class="card" id="demo">
      <h3>Book a 10-minute demo</h3>
      <p>See the exact missed-call flow.</p>

      <!-- Replace href with your Calendly link -->
      <a class="btn primary" href="https://calendly.com/YOUR-CALENDLY-LINK" target="_blank">
        Book Demo
      </a>
    </div>
  </div>

  <div class="section" id="how">
    <h2>How it works</h2>
    <ul class="bullets">
      <li>1. Customer calls — no answer</li>
      <li>2. Instant text follows up</li>
      <li>3. Lead is qualified</li>
      <li>4. Callback is booked</li>
    </ul>
  </div>

  <footer>
    © 2025 MissedCallAI • Built for HVAC & inbound sales teams
  </footer>

</div>

</body>
</html>
