<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Never Lose Another Hot Lead to a Missed Call</title>
  <meta name="description" content="AI instantly texts missed callers, qualifies them, and books callbacks automatically." />
  <style>
    :root{
      --bg:#0b0f19;
      --card:#111827;
      --muted:#9ca3af;
      --text:#f9fafb;
      --accent:#22c55e;
      --accent2:#60a5fa;
      --border:rgba(255,255,255,.08);
      --shadow:0 20px 60px rgba(0,0,0,.35);
      --radius:18px;
      --max:1100px;
    }
    *{box-sizing:border-box}
    body{
      margin:0;
      font-family: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial, "Apple Color Emoji","Segoe UI Emoji";
      background:
        radial-gradient(1200px 600px at 20% -10%, rgba(34,197,94,.25), transparent 60%),
        radial-gradient(900px 500px at 90% 10%, rgba(96,165,250,.20), transparent 55%),
        var(--bg);
      color:var(--text);
      line-height:1.45;
    }
    a{color:inherit}
    .container{max-width:var(--max); margin:0 auto; padding:24px}
    .nav{
      display:flex; align-items:center; justify-content:space-between;
      padding:14px 0;
    }
    .brand{display:flex; gap:10px; align-items:center; font-weight:700; letter-spacing:.2px}
    .logo{
      width:34px; height:34px; border-radius:10px;
      background: linear-gradient(135deg, rgba(34,197,94,.9), rgba(96,165,250,.9));
      box-shadow: var(--shadow);
    }
    .navlinks{display:flex; gap:16px; align-items:center}
    .navlinks a{font-size:14px; color:var(--muted); text-decoration:none}
    .navlinks a:hover{color:var(--text)}
    .btn{
      display:inline-flex; align-items:center; justify-content:center;
      padding:12px 16px; border-radius:14px;
      border:1px solid var(--border);
      background: rgba(255,255,255,.04);
      color:var(--text);
      text-decoration:none;
      font-weight:600;
      transition:.15s ease;
      cursor:pointer;
    }
    .btn:hover{transform:translateY(-1px); background: rgba(255,255,255,.06)}
    .btn.primary{
      background: linear-gradient(135deg, rgba(34,197,94,.95), rgba(34,197,94,.65));
      border:1px solid rgba(34,197,94,.35);
      color:#052e16;
    }
    .btn.primary:hover{filter:brightness(1.03)}
    .btn.secondary{
      border:1px solid rgba(96,165,250,.35);
      background: rgba(96,165,250,.08);
    }
    .hero{
      display:grid;
      grid-template-columns: 1.15fr .85fr;
      gap:26px;
      padding:28px 0 14px;
      align-items:stretch;
    }
    @media (max-width: 900px){
      .hero{grid-template-columns:1fr; padding-top:18px}
    }
    .pill{
      display:inline-flex; gap:10px; align-items:center;
      padding:8px 12px; border-radius:999px;
      border:1px solid var(--border);
      background: rgba(255,255,255,.03);
      color: var(--muted);
      font-size:13px;
      width:fit-content;
    }
    .dot{width:8px;height:8px;border-radius:50%;background:var(--accent); box-shadow:0 0 0 6px rgba(34,197,94,.14)}
    h1{font-size:44px; line-height:1.05; margin:14px 0 10px}
    @media (max-width: 520px){ h1{font-size:34px} }
    .sub{font-size:18px; color:#d1d5db; max-width:58ch}
    .bullets{
      margin:16px 0 22px; padding:0; list-style:none;
      display:grid; gap:10px;
    }
    .bullets li{
      display:flex; gap:10px; align-items:flex-start;
      color:#e5e7eb;
    }
    .check{
      margin-top:3px;
      width:18px; height:18px; border-radius:6px;
      background: rgba(34,197,94,.18);
      border:1px solid rgba(34,197,94,.35);
      display:inline-flex; align-items:center; justify-content:center;
      font-size:12px;
    }
    .ctaRow{display:flex; gap:12px; flex-wrap:wrap; align-items:center}
    .micro{margin-top:10px; color:var(--muted); font-size:13px}
    .card{
      background: rgba(17,24,39,.72);
      border:1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      overflow:hidden;
    }
    .demoCardHeader{
      padding:16px 16px 10px;
      border-bottom:1px solid var(--border);
      display:flex; justify-content:space-between; gap:10px; align-items:center;
    }
    .demoTitle{font-weight:700}
    .mini{color:var(--muted); font-size:13px}
    .demoBody{padding:14px 16px 16px}
    .step{
      display:flex; gap:12px; padding:12px;
      border:1px solid var(--border);
      border-radius:16px;
      background: rgba(255,255,255,.02);
      margin-bottom:10px;
    }
    .badge{
      min-width:30px; height:30px; border-radius:12px;
      display:flex; align-items:center; justify-content:center;
      background: rgba(96,165,250,.14);
      border:1px solid rgba(96,165,250,.25);
      color:#dbeafe;
      font-weight:800;
      font-size:13px;
    }
    .step b{display:block}
    .step span{color:var(--muted); font-size:13px}
    form{
      display:grid; gap:10px; margin-top:12px;
    }
    input{
      width:100%;
      padding:12px 12px;
      border-radius:14px;
      border:1px solid var(--border);
      background: rgba(255,255,255,.03);
      color:var(--text);
      outline:none;
    }
    input::placeholder{color:rgba(156,163,175,.8)}
    .section{padding:22px 0}
    .grid3{
      display:grid; grid-template-columns:repeat(3,1fr); gap:14px;
    }
    @media (max-width: 900px){ .grid3{grid-template-columns:1fr} }
    .feature{
      padding:16px;
      border-radius:18px;
      border:1px solid var(--border);
      background: rgba(255,255,255,.02);
    }
    .feature h3{margin:8px 0 6px; font-size:16px}
    .feature p{margin:0; color:var(--muted); font-size:14px}
    .kicker{color:var(--muted); font-size:13px; letter-spacing:.15px; text-transform:uppercase}
    h2{font-size:26px; margin:8px 0 12px}
    .twoCol{
      display:grid; grid-template-columns: 1fr 1fr; gap:14px; align-items:stretch;
    }
    @media (max-width: 900px){ .twoCol{grid-template-columns:1fr} }
    .pricing{
      padding:18px;
      border-radius:18px;
      border:1px solid rgba(34,197,94,.28);
      background: rgba(34,197,94,.06);
    }
    .pricing b{font-size:22px}
    .pricing p{color:#d1fae5; margin:8px 0 0}
    .faq details{
      border:1px solid var(--border);
      border-radius:16px;
      background: rgba(255,255,255,.02);
      padding:12px 14px;
      margin-bottom:10px;
    }
    .faq summary{cursor:pointer; font-weight:650}
    .faq p{margin:8px 0 0; color:var(--muted)}
    .footer{
      padding:22px 0 40px;
      color:var(--muted);
      font-size:13px;
      display:flex; justify-content:space-between; gap:12px; flex-wrap:wrap;
      border-top:1px solid var(--border);
      margin-top:18px;
    }
    .tagRow{display:flex; gap:8px; flex-wrap:wrap; margin-top:10px}
    .tag{
      font-size:12px; color:#e5e7eb;
      padding:6px 10px; border-radius:999px;
      background: rgba(255,255,255,.03);
      border:1px solid var(--border);
    }
  </style>
</head>

<body>
  <div class="container">
    <div class="nav">
      <div class="brand">
        <div class="logo" aria-hidden="true"></div>
        <div>MissedCallAI</div>
      </div>
      <div class="navlinks">
        <a href="#how">How it works</a>
        <a href="#pricing">Pricing</a>
        <a href="#faq">FAQ</a>
        <a class="btn secondary" href="#demo">Book demo</a>
      </div>
    </div>

    <div class="hero">
      <div>
        <div class="pill"><span class="dot"></span><span>Built for inbound-driven sales teams (HVAC, solar, roofing)</span></div>
        <h1>Never lose another hot lead to a missed call.</h1>
        <p class="sub">
          When you miss a call, your AI assistant texts the lead instantly, qualifies them with a few quick questions,
          and books a callback — automatically.
        </p>

        <ul class="bullets">
          <li><span class="check">✓</span><span><b>Instant follow-up</b> texts within seconds of a missed call.</span></li>
          <li><span class="check">✓</span><span><b>Qualification built-in</b> collects intent + urgency fast.</span></li>
          <li><span class="check">✓</span><span><b>Booked callbacks</b> routes warm leads to your calendar.</span></li>
        </ul>

        <div class="ctaRow">
          <a class="btn primary" href="#demo">See the 2-minute demo</a>
          <a class="btn" href="#how">How it works</a>
        </div>
        <div class="micro">Cancel anytime • No contracts • Live in minutes</div>

        <div class="tagRow">
          <div class="tag">No app for prospects</div>
          <div class="tag">Works while you’re busy</div>
          <div class="tag">Protects margins (usage-based)</div>
        </div>
      </div>

      <div class="card" id="demo">
        <div class="demoCardHeader">
          <div>
            <div class="demoTitle">Book a quick demo</div>
            <div class="mini">See the exact missed-call flow (10 minutes)</div>
          </div>
          <div class="mini">⭐ Early access</div>
        </div>
        <div class="demoBody">
          <div class="step">
            <div class="badge">1</div>
            <div>
              <b>Missed call detected</b>
              <span>Prospect calls • no answer • system triggers follow-up</span>
            </div>
          </div>
          <div class="step">
            <div class="badge">2</div>
            <div>
              <b>AI texts instantly</b>
              <span>Natural message • collects need + urgency</span>
            </div>
          </div>
          <div class="step">
            <div class="badge">3</div>
            <div>
              <b>Callback gets booked</b>
              <span>Warm lead routes to your scheduling link</span>
            </div>
          </div>

          <!-- Replace action with your form tool / Calendly link -->
          <form action="#" method="post" onsubmit="return false;">
            <input type="text" placeholder="Your name" aria-label="Your name" />
            <input type="email" placeholder="Work email" aria-label="Work email" />
            <input type="text" placeholder="Business type (HVAC, solar, etc.)" aria-label="Business type" />
            <a class="btn primary" href="#" onclick="alert('Replace this with your Calendly/booking link or form submit.'); return false;">
              Continue to booking
            </a>
            <div class="micro">Prefer not to book? Email us: <u>demo@yourdomain.com</u></div>
          </form>
        </div>
      </div>
    </div>

    <div class="section" id="how">
      <div class="kicker">How it works</div>
      <h2>Missed calls don’t lose deals — slow follow-up does.</h2>

      <div class="grid3">
        <div class="feature">
          <div class="badge" style="background:rgba(34,197,94,.16);border-color:rgba(34,197,94,.28);color:#d1fae5;">A</div>
          <h3>Instant text follow-up</h3>
          <p>Texts the caller within seconds, when they’re still engaged.</p>
        </div>
        <div class="feature">
          <div class="badge">B</div>
          <h3>Fast qualification</h3>
          <p>Asks short, professional questions to capture intent + urgency.</p>
        </div>
        <div class="feature">
          <div class="badge" style="background:rgba(255,255,255,.06);border-color:rgba(255,255,255,.14);color:#fff;">C</div>
          <h3>Booked callbacks</h3>
          <p>Routes warm leads straight to your calendar link (or “CALL NOW”).</p>
        </div>
      </div>
    </div>

    <div class="section" id="pricing">
      <div class="twoCol">
        <div class="card" style="padding:18px">
          <div class="kicker">Who it’s for</div>
          <h2>Perfect for inbound-driven sales</h2>
          <p style="color:var(--muted); margin:0 0 12px">
            Home services, appointment-based sales, and teams where one missed call can mean a missed sale.
          </p>
          <ul class="bullets" style="margin-top:12px">
            <li><span class="check">✓</span><span>Owners and reps who can’t answer every call</span></li>
            <li><span class="check">✓</span><span>Teams that want booked callbacks, not more admin work</span></li>
            <li><span class="check">✓</span><span>Anyone who wants a simple ROI story (recover 1 job/month)</span></li>
          </ul>
          <a class="btn secondary" href="#demo">Book the demo</a>
        </div>

        <div class="pricing">
          <div class="kicker" style="color:#bbf7d0">Simple monthly pricing</div>
          <b>Starting at $49/mo</b>
          <p>Most teams choose <b>$99/mo</b>. Usage-based messaging keeps costs fair and margins predictable.</p>
          <div class="tagRow" style="margin-top:14px">
            <div class="tag">Cancel anytime</div>
            <div class="tag">No contracts</div>
            <div class="tag">Live in minutes</div>
          </div>
          <div style="margin-top:14px">
            <a class="btn primary" href="#demo">See plans on demo</a>
          </div>
        </div>
      </div>
    </div>

    <div class="section faq" id="faq">
      <div class="kicker">FAQ</div>
      <h2>Quick answers</h2>

      <details>
        <summary>Do prospects need to download an app?</summary>
        <p>No. It’s SMS-based. Prospects just reply to a text like normal.</p>
      </details>

      <details>
        <summary>How fast can I get live?</summary>
        <p>Minutes. You can start
